interface:
  display_name: "하위 에이전트 오케스트레이션"
  short_description: "독립 이점이 있는 작업만 하위 에이전트에 배정합니다."
  default_prompt: "$orchestrate-subagents를 사용해 독립적으로 병렬화할 가치가 있는 작업만 하위 에이전트에 배정해줘."

policy:
  allow_implicit_invocation: true
