# 저장소 지침

## 지침 파일 언어

- `AGENTS.md`에 추가하는 모든 지침, 규칙, 섹션 제목, 레이블, 예시와 기타 설명문은 한국어로 작성합니다.
- 저장소 수준의 에이전트 지침은 한국어로 작성합니다. 스킬이 생성하는 사용자 대상 문서 및 출력은 사용자가 언어를 명시적으로 지정하면 해당 언어를 사용하고, 지정하지 않으면 한국어를 사용합니다.

## 스킬 소스 언어

- 모든 스킬 소스 파일을 한국어로 작성하고 유지합니다.
- 이 규칙은 `SKILL.md`, 스킬 메타데이터, 에이전트 설정, 템플릿, 예시와 스킬에 포함된 지침 설명문에 적용됩니다.
- 한국어 설명문 안의 코드, 명령어, 파일 경로, 식별자, API 이름, 모델 ID, 프로토콜 이름과 필수 고유명사는 정확히 보존합니다.
- 새 스킬을 추가하거나 기존 스킬을 수정할 때마다 `README.md`를 갱신합니다.

## 모든 스킬의 필수 언어 규칙

- 모든 `SKILL.md`에는 명시적인 출력 언어 섹션 또는 동등한 지침이 있어야 합니다. 이 지침은 사용자가 언어를 명시적으로 지정하면 해당 언어를 사용하고, 지정하지 않으면 스킬이 만드는 모든 사용자 대상 출력, 문서, 프롬프트, 보고서, 계획, 스펙과 기타 산출물을 한국어로 작성하도록 요구해야 합니다.
- 스킬을 만들거나 수정할 때 해당 스킬의 `SKILL.md`에 이 규칙을 추가하거나 갱신합니다. `AGENTS.md`에만 의존하지 않습니다.
- 이 규칙은 일반 문서와 최종 응답뿐 아니라 생성되는 프롬프트 문구에도 적용됩니다.

## 생성 문서와 출력

- 스킬이 문서, 프롬프트, 보고서, 계획, 스펙 또는 기타 사용자 대상 출력을 생성하거나 갱신하거나 제시할 때 사용자가 언어를 명시적으로 지정하면 해당 언어로 작성하고, 지정하지 않으면 한국어로 작성합니다.
- 실행 중 생성되는 모든 사용자 대상 설명문, 섹션 제목, 하위 섹션 제목, 레이블, 표, 체크리스트, 다이어그램과 템플릿에 이 규칙을 적용합니다.

## 섹션과 레이블

- 스킬이 문서 또는 인계 구조를 지정하면 필수 의미 섹션과 그 순서를 보존합니다.
- 의미와 순서를 유지하면서 섹션 이름과 레이블을 사용자가 명시적으로 지정한 언어로 작성하고, 지정하지 않으면 한국어로 작성합니다.
- 사용자가 언어를 지정하지 않았고 같은 의미를 한국어 레이블로 표현할 수 있으면 영어 섹션 이름을 출력하지 않습니다.

## 기술 토큰

- 사용자가 번역을 명시적으로 요청하지 않는 한 코드, 명령어, 파일 경로, 식별자, API 이름, 모델 ID, 프로토콜 이름과 필수 고유명사를 정확히 보존합니다.
- 주변 설명문을 번역한 뒤에도 Markdown 구조와 코드 블록을 유효하게 유지합니다.
